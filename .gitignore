.idea/
.vscode/
*.stl
# Exception: docs viewer STLs need to be tracked so GitHub Pages serves them.
!docs/stl/*.stl
*.test
*.out

# Dev artifacts at the repo root from `go build` / `go run` of unnamed packages.
/positioning
