.idea/
.vscode/
*.stl
# Exception: docs viewer STLs need to be tracked so GitHub Pages serves them.
!docs/stl/*.stl
*.test
*.out

# Coverage artifacts produced by scripts/coverage.sh and CI.
coverage.out
coverage.html

# Dev artifacts at the repo root from `go build` / `go run` of unnamed packages.
/positioning

# Generated fuzz corpus (per-package, not committed).
testdata/fuzz/

# Local-only planning / scratch docs. Kept out of git so they don't show
# up in pkg.go.dev or on the GitHub repo page.
/meta/
