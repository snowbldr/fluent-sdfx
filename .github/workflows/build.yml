name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  # Needed by codecov-action v5's OIDC tokenless upload path. Without this
  # permission the upload fails with "Token required - not valid tokenless
  # upload" on the v5 action against a public repo.
  id-token: write

jobs:
  build-and-test:
    name: build, vet, test, examples
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: true

      - name: go vet
        run: go vet ./...

      - name: go build (library + every example)
        run: go build ./...

      - name: go test
        run: go test ./...

      - name: go test (coverage)
        run: go test -coverprofile=coverage.out -covermode=atomic ./...

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage.out
          use_oidc: true
          fail_ci_if_error: false
