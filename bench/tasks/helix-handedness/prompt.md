I need a cylindrical core, radius 8 mm, 24 mm tall, centred on the origin,
with one helical rib wound around the outside of it.

The rib is a round bead, 2 mm diameter, and its centre line runs on the core
surface (radius 8) so it stands 1 mm proud. Single start, one turn every
12 mm of height, so two full turns over the 24 mm.

It has to be a right-hand helix, like a normal screw thread: looking down on
it from above, the rib turns clockwise as it goes down and away from you.
Start the rib on the +X side at mid height (z = 0).

The rib ends flush with the core: nothing hangs past the top or bottom face.
Model it as a helix that runs a little past both faces and is then sliced off
flat at z = +12 and z = -12, so each end is a sliced thread section, not a
rounded cap.
